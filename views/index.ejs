<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Barlow:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Lora:ital,wght@0,400..700;1,400..700&display=swap"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="styles/style.css" />
    <title>Weather Project</title>
  </head>
  <body>
    <div class="container">
      <div><h1>Today's Weather!</h1></div>
      <div>
        <form action="/submit" method="post">
          <label for="cityInput"></label>
          <input type="text" name="city" id="cityInput" placeholder="City name" /><br>
          <input type="submit" value="Search" id="searchBtn" />
        </form>
      </div>
      <%if(locals.data) {%>
      <div><h2>Current temperature: <%= Math.round(data.main.temp) %>°C</h2></div>
      <div><h3>Feels like: <%= Math.round(data.main.feels_like) %>°C</h3></div>
      <div>
        <p>Max/Min <br /><%= Math.round(data.main.temp_max) %>°C/<%= Math.round(data.main.temp_min) %>°C</p>
      </div>
      <div><p>Humidity: <%= Math.round(data.main.humidity) %>%</p></div>
      <%}%>
    </div>
  </body>
</html>
